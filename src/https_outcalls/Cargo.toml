[package]
name = "https_outcalls"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.8"
ic-cdk = "0.7"
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }
chrono = "0.4.26"

